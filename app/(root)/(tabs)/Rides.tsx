import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
    return(
        <SafeAreaView>
            <Text className="text-red-600">History</Text>
        </SafeAreaView>
    )
}

export default Rides;